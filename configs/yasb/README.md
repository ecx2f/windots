# YASB config notes

Before using `config.yaml`, update these values:

- `weather.options.api_key`: set your WeatherAPI key.
- `weather.options.location`: set your city/location.
- `home.options.menu_list`: adjust quick-access folders for your system.

The default paths are portable (`%USERPROFILE%`) and safe to customize.
